"use client"
import scss from "./BootsAll.module.scss";

const BootsAll = () => {
 return (
  <section className={scss.BootsAll}>
   <div className="container">
    <div className={scss.content}>BootsAll</div>
   </div>
  </section>
 );
};

export default BootsAll;
